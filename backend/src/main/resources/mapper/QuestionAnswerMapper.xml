<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sazare.mapper.QuestionAnswerMapper">

    <insert id="insertQuestionAnswer" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        insert into question_answers (
            question_id,
            answer_text,
            answer_type,
            primary_answer,
            sort_order,
            deleted,
            created_at,
            updated_at
        ) values (
            #{questionId},
            #{answerText},
            #{answerType},
            #{primaryAnswer},
            #{sortOrder},
            #{deleted},
            #{createdAt},
            #{updatedAt}
        )
    </insert>

    <select id="selectActiveAnswersByQuestionId" resultType="com.sazare.entity.QuestionAnswer">
        select
            id,
            question_id,
            answer_text,
            answer_type,
            primary_answer,
            sort_order,
            deleted,
            created_at,
            updated_at
        from question_answers
        where question_id = #{questionId}
          and deleted = false
        order by sort_order asc, id asc
    </select>

    <select id="selectActiveAnswersByQuestionIds" resultType="com.sazare.entity.QuestionAnswer">
        select
            id,
            question_id,
            answer_text,
            answer_type,
            primary_answer,
            sort_order,
            deleted,
            created_at,
            updated_at
        from question_answers
        where deleted = false
          and question_id in
          <foreach collection="questionIds" item="questionId" open="(" separator="," close=")">
              #{questionId}
          </foreach>
        order by question_id asc, sort_order asc, id asc
    </select>

    <update id="logicalDeleteByQuestionId">
        update question_answers
        set
            deleted = true,
            updated_at = current_timestamp
        where question_id = #{questionId}
          and deleted = false
    </update>
</mapper>
