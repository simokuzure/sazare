# TODO

更新时间：2026-08-13

## 已完成

### 项目基础与数据模型

- [x] 初始化 `backend + frontend` 单仓库、Spring Boot、Vite + React + TypeScript、Docker Compose（PostgreSQL、Redis）及开发启动说明。
- [x] 建立统一响应、业务异常、全局异常处理和健康检查接口 `GET /api/health`。
- [x] 完成标签、题目、答案、用户答题、错误类型、用户错误类型、复习及学习统计相关数据库模型。
- [x] 完成与当前表结构对应的 Entity、MyBatis Mapper、DTO、VO 和分页结构。

### 题目与 AI 生成

- [x] 实现标签分页查询 `GET /api/tags`，前端支持筛选、分页和页大小切换。
- [x] 完成短句和文章题目的 AI Prompt、固定 JSON 契约、业务校验及事务入库。
- [x] 实现 `AI_PROVIDER=mock|google` 配置切换，统一管理题目生成、评分、复习和日语纠错客户端。
- [x] 实现短句和文章 AI 生成接口：`POST /api/questions/ai-generations`、`POST /api/questions/article-ai-generations`。
- [x] 实现题目人工创建、分页列表、随机获取、详情、编辑、启用/停用和逻辑删除接口。
- [x] 使用 PostgreSQL `pgvector` 完成题目语义去重、HNSW 索引和历史向量回填接口 `POST /api/questions/embedding-backfills`。
- [x] 前端题目管理页已接入短句、文章题目的列表、详情、创建、编辑、启停和删除能力。

### 练习、评分与答题记录

- [x] 完成短句和文章答题评分的 Prompt、JSON 契约、结果校验、分数计算和容错处理。
- [x] 实现答题提交接口 `POST /api/questions/{questionId}/answers`，并将成功评分结果持久化至 `user_answers`。
- [x] 前端支持随机短句、文章生成与随机抽取、提交答案，以及展示总分、分项评分、错误分析和修改建议。
- [x] 实现当前用户答题记录分页查询和详情接口：`GET /api/user-answers`、`GET /api/user-answers/{id}`。
- [x] 前端答题记录页支持按短句、文章和日语纠错筛选，并在页内切换列表与详情。
- [x] 实现纯日语内容纠错 `POST /api/japanese-corrections`，包含独立 Prompt、AI JSON 校验、完整修订稿和前端练习入口。

### 错误记录、复习与统计

- [x] 建立两级错误类型、用户错误类型和答题错误记录模型；AI 错误仅作为候选，只有用户确认的错误进入薄弱项统计。
- [x] 实现错误类型、用户错误类型查询和答题错误确认接口，前端支持候选确认及已有用户错误类型复用。
- [x] 实现复习卡片、复习周期、复习题和复习尝试的持久化模型与 SM-2 调度。
- [x] 实现复习卡片列表、详情、提交复习答案和生成衍生题接口及前端错题复习流程。
- [x] 将短句、文章和纯日语纠错的已确认错误接入复习闭环。
- [x] 实现学习统计接口 `GET /api/learning-statistics` 和前端图表页，展示作答趋势、评分维度、已确认薄弱项及复习状态；翻译与纯日语纠错分开统计。

### 测试与文档

- [x] 补充题目、语义去重、答题评分、答题记录、错误类型、复习、学习统计、日语纠错、AI Prompt 与客户端单元测试。
- [x] 完成题目 API 设计、题目生成 Prompt 和答题评分 Prompt 文档。
- [x] 当前工作区后端 117 项测试通过，前端 TypeScript 与 Vite 生产构建通过。

## 待办事项

### P0：运行验证

- [ ] 使用真实 Google AI 完成短句生成与评分、文章生成与评分、复习题生成和纯日语纠错验证，并补充环境变量、失败排查和 mock 回退说明。
- [ ] 在本地 PostgreSQL 环境完成“生成题目 → 练习评分 → 确认错误 → 错题复习 → 学习统计”的端到端回归，覆盖短句、文章和纯日语纠错。
- [ ] 在浏览器中验证主要页面的桌面与窄屏交互，重点检查练习、错误确认、复习和统计图表。

### P1：质量与用户边界

- [ ] 设计并实现登录/用户身份识别，将当前 `LOCAL_DEFAULT` 范围切换为真实当前用户，并验证数据隔离。
- [ ] 补充 Controller/API 集成测试，覆盖参数校验、用户数据隔离、事务边界和主要失败场景。
- [ ] 补充前端关键流程测试，至少覆盖练习提交、错误确认、复习卡片状态切换和统计加载。
- [ ] 完善日志与运行诊断，避免输出 API Key、用户答案等敏感信息。
- [ ] 评估并优化前端构建产物拆包；当前主 JavaScript chunk 超过 Vite 默认的 500 kB 提示阈值。

### P2：学习体验

- [ ] 明确提前复习对 SM-2 间隔、到期时间和复习周期的影响后，再支持提前复习。
- [ ] 支持限时答题和语音输入。
- [ ] 扩展“根据词语使用日语解释含义”等题型；先明确评分标准、AI JSON 契约和数据模型。
- [ ] 在现有学习统计基础上评估个性化学习推荐，避免在需求未明确前引入复杂推荐算法。
