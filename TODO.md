# TODO

更新时间：2026-08-17

## 当前状态

- 核心学习闭环已实现：题目生成与管理、练习评分、答题记录、复习卡片、间隔复习和学习统计。
- 当前工作区已验证后端 160 项测试、前端 ESLint 和 Vite 生产构建；主要桌面页面已完成浏览器交互与布局核对。
- 真实 Google AI、本地 PostgreSQL 全流程和窄屏浏览器回归尚未完成，继续列为最高优先级。

## 已完成

### 项目基础与数据模型

- [x] 建立 `backend + frontend` 单仓库，接入 Spring Boot、MyBatis、PostgreSQL、Redis、Vite、React 和 TypeScript，并提供 Docker Compose 与本地启动说明。
- [x] 建立统一响应、业务异常、全局异常处理和健康检查接口 `GET /api/health`。
- [x] 完成题目、标签、答案、答题记录、错误类型、复习周期、复习尝试和学习统计相关的数据模型、Mapper、DTO、VO 与分页结构。

### 题库与 AI 生成

- [x] 实现标签分页查询，以及短句、文章题目的人工创建、分页筛选、随机获取、详情、编辑、启停和逻辑删除。
- [x] 完成短句与文章生成的 Prompt、固定 JSON 契约、业务校验和事务入库，支持 `AI_PROVIDER=mock|google` 切换。
- [x] 实现短句和文章生成接口：`POST /api/questions/ai-generations`、`POST /api/questions/article-ai-generations`。
- [x] 使用 PostgreSQL `pgvector` 实现同模型语义去重、HNSW 索引和历史向量回填接口 `POST /api/questions/embedding-backfills`。
- [x] 通过题材蓝图、历史生成上下文和重试约束提高文章主题、人物关系与表达方式的多样性。
- [x] 前端题目管理页已接入短句、文章题目的列表、详情、创建、编辑、启停和删除能力。

### 练习、评分与答题记录

- [x] 完成短句和文章评分的 Prompt、JSON 契约、结果校验、分数计算和容错处理，并通过 `POST /api/questions/{questionId}/answers` 持久化成功结果。
- [x] 前端支持随机抽题、AI 生成、提交答案，以及展示总分、分项评分、错误分析和修改建议。
- [x] 实现当前用户答题记录分页与详情接口：`GET /api/user-answers`、`GET /api/user-answers/{id}`；前端支持按短句、文章和日语纠错筛选。
- [x] 实现纯日语内容纠错 `POST /api/japanese-corrections`，包含独立 Prompt、结构化校验、完整修订稿和前端入口；标点不参与错误分析、评分和复习。

### 复习卡片与学习统计

- [x] 建立两级错误类型和答题错误记录；AI 分析仅作为候选，只有用户主动记录的内容进入复习卡片闭环。
- [x] 实现复习卡片列表、详情、复习历史、提交答案、生成衍生题和 SM-2 调度，并支持不限次数提前复习与每日 07:00 到期规则。
- [x] 将短句、文章和纯日语纠错接入统一复习闭环，并支持从练习评分、纠错结果和复习评分中手动添加或复用复习卡片。
- [x] 支持从列表和详情逻辑删除复习卡片；历史记录继续保留，再次添加同名内容时创建全新卡片和复习进度。
- [x] 衍生题按新题语义由 AI 选择场景与功能标签；文章复习句由 AI 重新判定标签，不继承文章体裁标签，并校验标签白名单、数量与类型。
- [x] 实现学习统计接口 `GET /api/learning-statistics` 和前端图表，展示连续与累计打卡、可切换的翻译与纯日语纠错练习表现、实时卡片状态及期间复习表现。

### 前端体验、测试与文档

- [x] 完成主要页面导航、筛选、列表、弹窗和响应式基础布局；统一练习页按钮与卡片布局，精简题目管理和答题记录列表展示。
- [x] 覆盖题目生成、语义去重、文章多样性、评分、记录、错误确认、复习、统计、日语纠错、AI Prompt 与客户端等核心单元测试。
- [x] 完成题目 API、题目生成 Prompt、评分 Prompt 与项目运行说明文档。

## 待办事项

### P0：真实环境回归

- [ ] 使用真实 Google AI 验证短句生成与评分、文章生成与评分、复习题生成、复习标签判定和纯日语纠错，并补充失败排查与 mock 回退说明。
- [ ] 在本地 PostgreSQL 环境完成“生成题目 → 练习评分 → 添加复习卡片 → 卡片复习 → 学习统计”端到端回归，覆盖短句、文章和纯日语纠错。
- [ ] 完成主要页面的窄屏浏览器回归，重点检查练习表单、复习卡片弹窗、答题记录表格和统计图表。

### P1：质量与用户边界

- [ ] 设计并实现登录与用户身份识别，将 `LOCAL_DEFAULT` 切换为真实当前用户并验证数据隔离。
- [ ] 补充 Controller/API 集成测试，覆盖参数校验、用户数据隔离、事务边界和主要失败场景。
- [ ] 补充前端关键流程测试，至少覆盖练习提交、复习卡片添加与复用、复习状态切换和统计加载。
- [ ] 完善日志与运行诊断，避免输出 API Key、用户答案等敏感信息。
- [ ] 评估前端构建拆包；当前主 JavaScript chunk 仍超过 Vite 默认的 500 kB 提示阈值。
